이전 버젼의 모든 오류를 해결하였지만, Invalid training data for sequence-to-sequence classification network. For multiple
observations, the responses must be a cell array of categorical row vectors. For a
single observation, the responses can be also a single categorical row vector.
라는 오류가 쳐 뜨네요 시12발 좆같노
이거 고치려면 train_transformer_mode 건드려야 하는데 이거 건드릴 때 마다 predict도 건드려야 해서 너무 좆같음 ㅋㅋ